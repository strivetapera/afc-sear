{
  "name": "@afc-sear/api",
  "version": "0.1.0",
  "private": true,
  "description": "Backend API scaffold for church platform services",
  "scripts": {
    "dev": "tsx watch src/server.ts",
    "build": "tsc -p tsconfig.build.json",
    "start": "node dist/server.js",
    "typecheck": "tsc -p tsconfig.json"
  },
  "dependencies": {
    "@afc-sear/auth": "^0.1.0",
    "@afc-sear/types": "0.1.0",
    "@fastify/cors": "^11.2.0",
    "fastify": "^5.6.1",
    "fastify-plugin": "^5.1.0"
  },
  "devDependencies": {
    "tsx": "^4.20.6"
  }
}
