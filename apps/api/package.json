{
  "name": "@afc-sear/api",
  "version": "0.1.0",
  "private": true,
  "description": "Backend API scaffold for church platform services",
  "scripts": {
    "dev": "tsx watch src/server.ts",
    "test": "vitest run",
    "build": "npm run --workspace @afc-sear/auth build && tsc -p tsconfig.build.json",
    "start": "tsx src/server.ts",
    "typecheck": "tsc -p tsconfig.json"
  },
  "dependencies": {
    "@afc-sear/auth": "^0.1.0",
    "@afc-sear/types": "0.1.0",
    "@fastify/cors": "^11.2.0",
    "bullmq": "^5.71.0",
    "fastify": "^5.6.1",
    "fastify-plugin": "^5.1.0",
    "ioredis": "^5.10.1",
    "tsx": "^4.20.6"
  },
  "devDependencies": {
    "vitest": "^4.1.1"
  }
}
